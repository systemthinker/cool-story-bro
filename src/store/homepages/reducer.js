const initialState = {
    homepages : [
        {
            homepageOneTest: "test",
        },
    ]
}

export default (state = initialState, action) => {
    switch (action.type) {
      
  
      default:
        return state;
    }
  };
